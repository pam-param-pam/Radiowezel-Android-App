<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.snowcorp.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="dev.pamparampam.myapplication.login.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name="dev.pamparampam.myapplication.login.LoginActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="dev.pamparampam.myapplication.login.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name="dev.pamparampam.myapplication.login.RegisterActivity"
            android:label="@string/app_name"
            android:parentActivityName="dev.pamparampam.myapplication.login.LoginActivity"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.pamparampam.myapplication.login.LoginActivity" />
        </activity>

        <activity
            android:name="dev.pamparampam.myapplication.login.EmailVerify"
            android:label="@string/app_name"
            android:parentActivityName="dev.pamparampam.myapplication.login.LoginActivity"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.pamparampam.myapplication.login.LoginActivity" />
        </activity>
    </application>

</manifest>